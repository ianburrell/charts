apiVersion: v1
kind: Secret
metadata:
  name: {{ template "metabase.fullname" . }}
  labels:
    app: {{ template "metabase.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  postgresql-user: {{ .Values.postgresql.postgresUser | b64enc | quote }}
